function best(prices,newBuyPrice,buyPrice ){
   let buyPrice = 0;
   if(prices.length < 2){
      let buyPrice = 0;
      for (let i = 0; i < prices.length-1; i++)
         for (let t = 0; t < prices.length; t++)
   } else if(i < t)
          let newBuyPrice = prices[i] - prices[j]
        } else if { newBuyPrice > buyPrice)
         lnewBuyPrice = buyPrice
   
          }
         }
   }
   return buyPrice;
}

   
   

 console.log(best([1, 2, 3, 4, 5]));
